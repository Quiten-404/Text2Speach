name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java 8
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '8'
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev \
          libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
          pip install --upgrade pip
          pip install buildozer cython
      
      - name: Setup Android SDK
        run: |
          mkdir -p ~/.buildozer/android/platform/android-sdk/tools/bin
          cd ~/.buildozer/android/platform/android-sdk
          wget -q https://dl.google.com/android/repository/sdk-tools-linux-4333796.zip
          unzip -q sdk-tools-linux-4333796.zip
          rm sdk-tools-linux-4333796.zip
          yes | ~/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager --licenses || true
          ~/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager "platform-tools" "platforms;android-30" "build-tools;30.0.3" || true
      
      - name: Setup Android NDK 25c
        run: |
          cd ~/.buildozer/android/platform
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip
          rm android-ndk-r25c-linux.zip
      
      - name: Fix missing llvm-rs-cc
        run: |
          # Создаём символическую ссылку для llvm-rs-cc (нужен для сборки)
          NDK_DIR=~/.buildozer/android/platform/android-ndk-r25c
          if [ ! -f $NDK_DIR/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-rs-cc ]; then
            ln -s $NDK_DIR/toolchains/llvm/prebuilt/linux-x86_64/bin/clang $NDK_DIR/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-rs-cc
          fi
      
      - name: Build APK
        run: |
          export ANDROID_NDK_HOME=~/.buildozer/android/platform/android-ndk-r25c
          buildozer android debug
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Text2Speach-App
          path: bin/*.apk
